name: Sync

on:
  workflow_run:
    workflows:
    - Compile Mac
    types:
    - completed
    branches:
    - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Sync
      uses: BetaHuhn/repo-file-sync-action@v1
      with:
        GH_PAT: ${{ secrets.GH_PAT }}
        SKIP_PR: true